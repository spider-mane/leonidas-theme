{##
 # Renders provided markup in between markup rendered in a loop.
 #}
{% macro between(loop, count, output) %}
  {% if not loop.last and loop.index is divisible by(count) %}
    {{ output }}
  {% endif %}
{% endmacro %}
